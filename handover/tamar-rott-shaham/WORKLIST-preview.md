<!-- Generated by `python scripts/handover.py "Tamar Rott Shaham" --dry-run`. A preview: what one
     `python update.py` finds for Tamar Rott Shaham from public records alone, with none of
     the CONFIRM values in config.yaml filled in yet. Every count here is a floor. -->

# What still needs you

Regenerated by `python update.py`. **Open items only** — a section that is
not here is done, and nothing on this page is a general instruction. The
how-to for every item below is [docs/SETUP.md](docs/SETUP.md); the live
reading of each external surface is [tasks/identity_audit.md](tasks/identity_audit.md).

## Start here

The page below is ordered by leverage and citation count, which is the right
order to read it in and not the order to work it in. This is that order, with
what each item costs — the one thing a section cannot say about itself,
because it does not know what else is open. Each line names the section that
holds the instructions; nothing here repeats them.

**A command, and nothing to decide.** Any day, in any order: these drain
backlogs the rest of the page is waiting on, and the run does the work.

1. **Wikidata — 16 statement gaps on Q125198037** — `python scripts/wikidata_apply.py --papers --limit 10`, repeated. The monthly CI leg refuses to touch new papers while a backlog this size exists, so this is the one item that turns maintenance back on.

**One edit each, and each one closes a section outright.** This is where
the page gets visibly shorter.

2. **ORCID is missing 28 of your 28 papers** — one BibTeX upload. Highest leverage on the page — Semantic Scholar and OpenAlex both re-cluster off ORCID, so this is the fix that makes other sections shrink without you.
3. **Papers whose full text nothing can fetch (4)** — drop the PDF you already have into `data/fulltext/` (gitignored, so it stays on your machine and only the sidecar it produces is committed).

**As much as you have patience for.** Per-paper clicking, because
there is no write API behind either surface — and both are ordered so
that stopping early still captures most of the value.

4. **arXiv journal-ref missing (12 papers)** — save <https://arxiv.org/user> and feed it to `identity_tasks.py --user-page` first: two minutes, once, and it turns every hunt-by-eye row into a one-click link. Then the top few and stop — that section argues its own case honestly.
5. **Semantic Scholar — 2 papers on a second author record** — one paste per paper into the Add Papers form, highest-citation first; every URL is in the section.

## Coverage: Google Scholar and the corpus disagree

Scholar lists **30** works and matched **26** of the corpus's **28**. Scholar is
the one list of your papers that is built by a different process, so it is the
only check that can see a paper this pipeline never received.

Every bucket in full, including what is truncated below:
[`build/scholar_diff.json`](build/scholar_diff.json) — local only, because
`build/` is not committed. `python update.py --step audit` writes it, and after
a fresh clone that is the one command between you and the file.

### 1 paper absent from the source bibliography

Not in the corpus and not rejected — they never arrived. The bibliography
is this pipeline's only input, so the fix is one entry there; adding them
to `data/` would be overwritten on the next run. A BibTeX entry for each,
resolved from arXiv, Crossref or Semantic Scholar where any of them has
it, is in [`tasks/bib_missing.md`](tasks/bib_missing.md) — check the
author list before pasting: it is the index's, not yours.

- [ ] 18 cites — 2025 — Token Entanglement in Subliminal Learning

### 1 paper whose title does not appear on your Scholar profile

That is all this check knows, and the heading says so deliberately. It reads
the profile listing, which shows **one title per record** — so a paper Scholar
has folded into another record is indistinguishable here from a paper Scholar
does not have. Both look like a title that is not in the list.

**So check for a merge before adding anything.** Scholar merges a call for
papers into the findings paper of the same workshop, and a preprint into its
retitled successor — the citations are all on the surviving record, which is
the outcome you want. Adding the folded paper by hand does not recover
anything; it creates a second record that splits future citations.

Open <https://scholar.google.com/citations?user=YRJ-ePMAAAAJ&view_op=list_works&sortby=pubdate> and look for
the related record — the findings paper, the newer title. If your paper is
inside it, decline the line here and you will not be asked again. Only if
nothing on the profile covers it is *+ → Add article manually* the fix.

Declining is one line in [`data/declines.yaml`](data/declines.yaml) under `items:`.

- [ ] 0 cites — 2026 — From Activation to Specificity: Automating Counterfactual  <https://arxiv.org/abs/2605.23895>

### 1 paper whose bibliography title is behind arXiv

arXiv states the title Scholar shows, so the source entry is the stale
one and there is nothing to decide: correct the title in the source
bibliography and re-run. Until then the two surfaces answer a title query
differently, which is the exact failure this repo exists to prevent.

- [ ] `the-dual-mechanisms-of-spatial-reasoning-in-vision-language`
      - arXiv and Scholar: The Dual Mechanisms of Spatial Variable Binding in Visio
      - the .bib entry:    The Dual Mechanisms of Spatial Reasoning in Vision-Langu

### 2 papers listed twice on Scholar

Two rows for one paper splits its citation count. Nothing in this repo can
fix it: *select both on your profile → Merge*.

- [ ] `a-multimodal-automated-interpretability-agent` — the second row reads 'OpenMAIA: a Multimodal Automated Interpretability Ag'
- [ ] `brainexplore-large-scale-discovery-of-interpretable-visual-r` — the second row reads 'From Activation to Causality: Discovery of Causal Vi'

## Identity surfaces (5 open)

Each is blocked on an account you are logged into, not on knowing what to
do. `python scripts/identity_tasks.py` regenerates every payload under
`tasks/` — committed, so browsable on GitHub.

### ORCID is missing 28 of your 28 papers

Highest leverage on this page. Semantic Scholar's disambiguation and
OpenAlex's profile merges are both ORCID-driven, so this is the one fix that
makes the others more likely to fix themselves.

One upload, not one form per paper. At <https://orcid.org/my-orcid#works>:
*+ Add → Add BibTeX → Choose file* →
[`tasks/orcid_missing.bib`](tasks/orcid_missing.bib) (only the missing ones) or
[`tasks/orcid_import.bib`](tasks/orcid_import.bib) (all of them; ORCID groups on
shared identifiers, so re-importing what is already there merges rather than
duplicates). It previews the entries and you confirm — nothing lands unseen.
Why it matters, once:
[docs/SETUP.md §1](docs/SETUP.md#1-orcid--populate-it-then-wire-it-everywhere).

- [ ] 959 cites — SinGAN: Learning a Generative Model From a Single Natural Image
- [ ] 139 cites — Fine-Tuning Enhances Existing Mechanisms: A Case Study on Entity T
- [ ] 91 cites — Spatially-Adaptive Pixelwise Networks for Fast Image Translation
- [ ] 69 cites — A Multimodal Automated Interpretability Agent
- [ ] 65 cites — xUnit: Learning a Spatial Activation Function for Efficient Image 
- [ ] 48 cites — A Vision Check-up for Language Models
- [ ] 38 cites — SketchAgent: Language-Driven Sequential Sketch Generation
- [ ] 38 cites — FIND: A Function Description Benchmark for Evaluating Interpretabi
- … and 20 more in [`tasks/orcid_missing.md`](tasks/orcid_missing.md)

### Semantic Scholar — 2 papers on a second author record

Every S2-backed tool (Elicit, Consensus, SciSpace, most literature agents)
resolves you to one page, so each currently sees about half the corpus.
Support has already been asked to merge the two records and declined, so the
self-service route is the only one: a claimed page can pull papers across one
at a time.

1. Open your claimed page: <https://www.semanticscholar.org/author/3459255>
2. *Edit Author Page → Add Papers*.
3. Paste a paper's S2 URL, pick it, and choose *the author is correct, but the
   paper is missing from my author page*. Changes appear in about 24 hours.

Highest-citation first, so stopping early still captures most of the loss.
**Do not claim the second page as well** — a second claimed record is harder to
undo than an unclaimed one, and it makes the split look deliberate.

- [ ] 35 cites — Automation in Interior Space Planning: Utilizing Conditi — <https://www.semanticscholar.org/paper/259954034>
- [ ] 0 cites — Edge preserving multi-modal registration based on gradie — <https://www.semanticscholar.org/paper/14425695>

### Wikidata — 16 statement gaps on Q125198037

Now automatic, and it does **not** need an autoconfirmed account — that is a
QuickStatements rule, not a MediaWiki one. Create a bot password once at
<https://www.wikidata.org/wiki/Special:BotPasswords> (grants: edit existing
pages, create/edit pages), export `WIKIDATA_BOT_USER` and
`WIKIDATA_BOT_PASSWORD`, then:

```bash
python scripts/wikidata_apply.py            # dry run: exactly what changes
python scripts/wikidata_apply.py --apply    # write it
```

### Wikidata — 27 of your papers have no item

Same bot password, and the same statements as the QuickStatements batch in
`tasks/wikidata_papers.qs` — which is now only the fallback. This is where
`Q125198037` gets the incoming author
links that make a Scholia profile and a SPARQL-answerable corpus exist at
all.

1 more have no item either and are not in the command below:
they carry neither a DOI nor an arXiv id, so there is no key to check
Wikidata against and creating one risks a duplicate nobody can find. They
arrive here once the paper is deposited anywhere.

```bash
python scripts/wikidata_apply.py --papers                    # what it would create
python scripts/wikidata_apply.py --papers --apply --limit 10  # ten of them
```

In batches, and this is the reason: ten items finds a wrong statement on item
3 rather than on item 103, and an item is harder to retract than anything else
here. Each one is recorded in `data/wikidata_created.yaml` as it lands, so
stopping and resuming creates nothing twice — the query service lags hours
behind the edit and that file is what covers the gap.

Once this list is empty the monthly CI run keeps up with new papers by itself.
It refuses while a backlog exists, so it is doing nothing until you start.
Cautions worth reading once: [`tasks/wikidata_followup.md`](tasks/wikidata_followup.md).

## arXiv journal-ref missing (12 papers)

**It is a metadata edit, not a new version.** No recompile, no file upload,
no new version number, no re-announcement — v2 stays v2, per arXiv's own
help page. That is the whole cost, about a minute each, and it is worth
knowing because the size of this list is not the size of the job.

The form is per-paper and lives behind your account: open
<https://arxiv.org/user>, find the row, follow its *journal ref* link.
There is no paste-an-identifier page — `/jref` on its own redirects to that
list — which is also why no script can do this for you.

**Five minutes first, if you are doing more than a couple.** 0 of the
12 rows have a direct link into their own form;
the other 12 have to be found by eye on that list. The id the
form is addressed by is only ever shown on your own articles page, and arXiv's
`robots.txt` disallows fetching it — so the route is a copy you save:

1. Sign in and open <https://arxiv.org/user>.
2. Save the page — ⌘S, *Page Source* is enough.
3. `python scripts/identity_tasks.py --user-page ~/Downloads/arxiv-user.html`

Submission ids never change, so this is once and not per run, and nothing is
requested on your behalf at any point — the code reads the file you saved.
After it, every entry in [`tasks/arxiv_jref.md`](tasks/arxiv_jref.md) opens
its own form.

**What it buys, honestly ranked.**

1. *Weak here.* Scholar merges preprint and published versions largely on
   venue agreement, and a venue-less arXiv record can stay a separate
   cluster with the citations split across the two.
   Measured on your own profile: **2 split pairs out of 28**, so for this
   corpus that is mostly already handled — do not do this for that reason.
2. **The arXiv DataCite record gains a `container-title`.** This is the real
   one and it is not visible on Scholar at all: that field is what flows to
   OpenAlex, to ORCID auto-update, and to every Crossref-derived tool, and
   a venue-less record is filtered out by anything ranking on venue.
3. **Answer engines cite venue as authority.** "Published at ACL 2024" in
   the metadata is what makes a model's answer name the venue instead of
   calling it a preprint.

**Recommendation:** the top few, when you are already logged in, and stop.
There is no write API, so the clicking is the one part of this list code
cannot take off you — but the typing is not: both field values are below,
per paper, built from the publisher's own bibtex. The same for all
12 is in [`tasks/arxiv_jref.md`](tasks/arxiv_jref.md).

- [ ] **959 cites** — SinGAN: Learning a Generative Model From a Single Natural Image
      - the form: find `1905.01164` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/1905.01164))
      - `Journal-ref:` — not derivable from the bibliography (venue is *IEEE International Conference on Computer Vision*); type the proceedings title yourself
      - `Journal version DOI:` `10.1109/ICCV.2019.00467`
- [ ] **139 cites** — Fine-Tuning Enhances Existing Mechanisms: A Case Study on Entity Tracking
      - the form: find `2402.14811` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/2402.14811))
      - `Journal-ref:` — not derivable from the bibliography (venue is *ICLR 2024*); type the proceedings title yourself
      - `Journal version DOI:` — none minted, leave blank
- [ ] **91 cites** — Spatially-Adaptive Pixelwise Networks for Fast Image Translation
      - the form: find `2012.02992` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/2012.02992))
      - `Journal-ref:` — not derivable from the bibliography (venue is *Computer Vision and Pattern Recognition*); type the proceedings title yourself
      - `Journal version DOI:` `10.1109/CVPR46437.2021.01464`
- [ ] **69 cites** — A Multimodal Automated Interpretability Agent
      - the form: find `2404.14394` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/2404.14394))
      - `Journal-ref:` — not derivable from the bibliography (venue is *ICML 2024*); type the proceedings title yourself
      - `Journal version DOI:` — none minted, leave blank
- [ ] **65 cites** — xUnit: Learning a Spatial Activation Function for Efficient Image Restoration
      - the form: find `1711.06445` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/1711.06445))
      - `Journal-ref:` — not derivable from the bibliography (venue is *2018 IEEE/CVF Conference on Computer Vision and Pattern Recognition*); type the proceedings title yourself
      - `Journal version DOI:` `10.1109/CVPR.2018.00258`
- [ ] **48 cites** — A Vision Check-up for Language Models
      - the form: find `2401.01862` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/2401.01862))
      - `Journal-ref:` — not derivable from the bibliography (venue is *Computer Vision and Pattern Recognition*); type the proceedings title yourself
      - `Journal version DOI:` `10.1109/CVPR52733.2024.01366`
- [ ] **38 cites** — SketchAgent: Language-Driven Sequential Sketch Generation
      - the form: find `2411.17673` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/2411.17673))
      - `Journal-ref:` — not derivable from the bibliography (venue is *Computer Vision and Pattern Recognition*); type the proceedings title yourself
      - `Journal version DOI:` `10.1109/CVPR52734.2025.02175`
- [ ] **29 cites** — Catch-A-Waveform: Learning to Generate Audio from a Single Short Example
      - the form: find `2106.06426` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/2106.06426))
      - `Journal-ref:` — not derivable from the bibliography (venue is *Neural Information Processing Systems*); type the proceedings title yourself
      - `Journal version DOI:` — none minted, leave blank
- [ ] **16 cites** — Deformation Aware Image Compression
      - the form: find `1804.04593` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/1804.04593))
      - `Journal-ref:` — not derivable from the bibliography (venue is *2018 IEEE/CVF Conference on Computer Vision and Pattern Recognition*); type the proceedings title yourself
      - `Journal version DOI:` `10.1109/CVPR.2018.00260`
- [ ] **3 cites** — Internal Diverse Image Completion
      - the form: find `2212.10280` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/2212.10280))
      - `Journal-ref:` — not derivable from the bibliography (venue is *CVPRW 2023*); type the proceedings title yourself
      - `Journal version DOI:` `10.1109/CVPRW59228.2023.00072`
- [ ] **2 cites** — Automated Detection of Visual Attribute Reliance with a Self-Reflective Agent
      - the form: find `2510.21704` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/2510.21704))
      - `Journal-ref:` — not derivable from the bibliography (venue is *Neural Information Processing Systems*); type the proceedings title yourself
      - `Journal version DOI:` — none minted, leave blank
- [ ] **0 cites** — Edge preserving multi-modal registration based on gradient intensity self-similarity
      - the form: find `1412.3914` on <https://arxiv.org/user> → its *journal ref* link ([abs](https://arxiv.org/abs/1412.3914))
      - `Journal-ref:` — not derivable from the bibliography (venue is *IEEE Convention of Electrical and Electronics Engineers in Israel*); type the proceedings title yourself
      - `Journal version DOI:` `10.1109/EEEI.2014.7005886`

`Report number:` stays blank on all of them: it means an *institutional* preprint
number (a lab's own report series) and none of these has one.

## Hugging Face paper page missing (12)

Log in to Hugging Face first: an unauthenticated visit creates nothing
(verified, 0 of 50). Visiting the URL while logged in *is* the action --
there is no form.

Full list, clickable: `tasks/hf_worklist.md`. Re-read the pages live
after a session of clicking: `python scripts/audit_identity.py --no-names`.

- [ ] <https://hf.co/papers/2012.02992>  (91 cites)
- [ ] <https://hf.co/papers/2505.14685>  (34 cites)
- [ ] <https://hf.co/papers/2307.03637>  (34 cites)
- [ ] <https://hf.co/papers/2106.06426>  (29 cites)
- [ ] <https://hf.co/papers/1804.04593>  (16 cites)
- [ ] <https://hf.co/papers/2212.10280>  (3 cites)
- [ ] <https://hf.co/papers/2603.20101>  (2 cites)
- [ ] <https://hf.co/papers/2603.22278>  (2 cites)
- [ ] <https://hf.co/papers/2510.21704>  (2 cites)
- [ ] <https://hf.co/papers/2212.01589>  (2 cites)

## Hugging Face page indexed but not claimed by you (10)

Claims go through moderation and Hugging Face only publishes the
author→user link once it is granted, so a request already submitted is
invisible from outside and would otherwise be listed here again. If you
have already asked for one of these, add its arXiv id to
`hf_claim_requested` in `data/overrides.yaml` and it moves to *pending*
in `tasks/hf_worklist.md` instead of back onto this list.

Full list and the other buckets: `tasks/hf_worklist.md`.

- [ ] <https://hf.co/papers/1905.01164>  (959 cites)
- [ ] <https://hf.co/papers/2402.14811>  (139 cites)
- [ ] <https://hf.co/papers/1711.06445>  (65 cites)
- [ ] <https://hf.co/papers/2401.01862>  (48 cites)
- [ ] <https://hf.co/papers/2411.17673>  (38 cites)
- [ ] <https://hf.co/papers/2309.03886>  (38 cites)
- [ ] <https://hf.co/papers/2602.20021>  (33 cites)
- [ ] <https://hf.co/papers/2605.12485>  (0 cites)
- [ ] <https://hf.co/papers/2606.03715>  (0 cites)
- [ ] <https://hf.co/papers/2605.23895>  (0 cites)

## Sidecars not yet drafted (28/28)

**Not yours.** Drafting reads each paper's full text and writes claims,
scope and glosses into a draft file — agent work, and the queue drains
when you ask an agent for a batch or when a full run takes one. It is here
so the number is visible, not so you will do it. What comes back is the
section above, and that one is yours.

```bash
python scripts/draft_sidecars.py --review      # every paper: live, draft, or neither
python scripts/draft_sidecars.py --limit 20    # queue the next 20 (then an agent fills them)
python scripts/draft_sidecars.py --ingest      # fold the answers in
```

`--review` is the whole list; the six below are the top of it by
citations, which is where drafting pays. A draft lands in
`data/sidecars/drafts/<slug>.md` and nothing reads it until you
`--accept` it, which moves it to `data/sidecars/<slug>.md` — the
published one, and the only one the site builds from.

`update.py` also drafts a batch on every run, so this number falls on
its own.

- `singan-learning-a-generative-model-from-a-single-natural-ima` — 959 cites — SinGAN: Learning a Generative Model From a Single Natura
- `fine-tuning-enhances-existing-mechanisms-a-case-study-on-ent` — 139 cites — Fine-Tuning Enhances Existing Mechanisms: A Case Study o
- `spatially-adaptive-pixelwise-networks-for-fast-image-transla` — 91 cites — Spatially-Adaptive Pixelwise Networks for Fast Image Tra
- `a-multimodal-automated-interpretability-agent` — 69 cites — A Multimodal Automated Interpretability Agent
- `xunit-learning-a-spatial-activation-function-for-efficient-i` — 65 cites — xUnit: Learning a Spatial Activation Function for Effici
- `a-vision-check-up-for-language-models` — 48 cites — A Vision Check-up for Language Models

## Papers whose full text nothing can fetch (4)

Every one of these is a real paper that is not on arXiv, so there is no
HTML rendering and no open PDF to extract — a Nature paywall, an Elsevier
page that serves an open-access licence to browsers and 403s to everything
else, an SSRN download behind a click. They are not slow, they are blocked,
and no rerun will change that.

You are an author on each of them, so you already have the PDFs. Drop each one in as
`data/fulltext/<slug>.pdf` — the directory is gitignored, so the PDF stays
on your machine and only the sidecar it produces is committed. That path is
read before any network source, so the next run picks it up and the paper
joins the drafting queue.

- [ ] **Automation in Interior Space Planning: Utilizing Conditional** — 35 cites, Buildings
      - get it from <https://doi.org/10.3390/buildings13071793>
      - save it as `data/fulltext/automation-in-interior-space-planning-utilizing-conditional.pdf`
- [ ] **Deep Self-Dissimilarities as Powerful Visual Fingerprints** — 10 cites, Neural Information Processing Systems
      - no landing page known — wherever your own copy is
      - save it as `data/fulltext/deep-self-dissimilarities-as-powerful-visual-fingerprints.pdf`
- [ ] **Visualizing Image Priors** — 8 cites, European Conference on Computer Vision
      - get it from <https://doi.org/10.1007/978-3-319-46466-4_9>
      - save it as `data/fulltext/visualizing-image-priors.pdf`
- [ ] **GANs Spatial Control via Inference-Time Adaptive Normalizati** — 7 cites, IEEE Workshop/Winter Conference on Applications of Computer Vision
      - get it from <https://doi.org/10.1109/WACV51458.2022.00011>
      - save it as `data/fulltext/gans-spatial-control-via-inference-time-adaptive-normalizati.pdf`

