{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/borgr/paper-geo/schema/papers.schema.json",
  "title": "data/papers.yaml",
  "description": "Fully generated by scripts/collect.py -- never hand-edit. Corrections go in data/overrides.yaml, which is applied on every run. Identifiers are the source of truth; the links map is derived from them each run so it cannot drift.",
  "type": "object",
  "required": [
    "papers"
  ],
  "additionalProperties": false,
  "properties": {
    "generated_by": {
      "type": "string"
    },
    "papers": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/paper"
      }
    }
  },
  "$defs": {
    "paper": {
      "type": "object",
      "required": [
        "slug",
        "title"
      ],
      "additionalProperties": false,
      "properties": {
        "slug": {
          "type": "string",
          "pattern": "^[a-z0-9-]+$",
          "description": "Stable key: sidecar filename and site path."
        },
        "key": {
          "type": [
            "string",
            "null"
          ],
          "description": "BibTeX key from the source bibliography."
        },
        "title": {
          "type": "string"
        },
        "authors": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "authors_truncated": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "The bibliography's author field ended in 'and others' (BibTeX et al.), so the list here is incomplete. Set by scripts/common.py:authors_truncated. Load-bearing for the authorship gate: a truncated list cannot be used to conclude a paper is not yours, so the gate falls back to arXiv's full author list instead of excluding. Cleared once a full list is obtained."
        },
        "authors_from_arxiv": {
          "type": "object",
          "description": "The stored author list came from arXiv, not from the bibliography, because the gate was about to drop this paper. `was`/`now` are the two list lengths and `why` says which shape triggered it. Kept in the committed data rather than only in a log, because it is the record of a paper that was recovered from a silent drop -- if the recovery ever regresses the paper vanishes, and this field is what makes the diff say so.",
          "additionalProperties": false,
          "properties": {
            "was": {"type": "integer"},
            "now": {"type": "integer"},
            "why": {"type": "string"}
          }
        },
        "year": {
          "type": [
            "integer",
            "null"
          ]
        },
        "venue": {
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "type": [
            "string",
            "null"
          ],
          "description": "BibTeX entry type."
        },
        "arxiv": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^\\d{4}\\.\\d{4,5}$"
        },
        "doi": {
          "description": "Only a DOI a source actually asserted. The arXiv DataCite DOI is derivable from arxiv and so is filled in at the point of use by common.paper_doi(), not stored here, where it could shadow a publisher DOI registered later.",
          "type": [
            "string",
            "null"
          ]
        },
        "acl": {
          "type": [
            "string",
            "null"
          ]
        },
        "s2_corpus_id": {
          "type": [
            "integer",
            "null"
          ]
        },
        "url": {
          "type": [
            "string",
            "null"
          ]
        },
        "links": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Derived from the identifiers above on every run. Consumed as the JSON-LD sameAs array: what makes an engine treat ten URLs as one work rather than ten pages. Sidecar links_extra adds what cannot be derived."
        },
        "abstract": {
          "type": [
            "string",
            "null"
          ]
        },
        "abstract_source": {
          "type": "string",
          "description": "Set when backfilled rather than taken from the bibliography."
        },
        "bibtex": {
          "type": [
            "string",
            "null"
          ],
          "description": "Verbatim from the source bibliography, never regenerated: a regenerated key would differ from the one people already cite."
        },
        "citations": {
          "type": [
            "integer",
            "null"
          ],
          "description": "Semantic Scholar count. Used only for prioritising work."
        },
        "arxiv_journal_ref": {
          "type": [
            "string",
            "null"
          ],
          "description": "Null means the fix Scholar needs to match citations is missing."
        },
        "arxiv_doi": {
          "type": [
            "string",
            "null"
          ]
        },
        "arxiv_comment": {
          "type": [
            "string",
            "null"
          ],
          "description": "Often names the venue that belongs in journal_ref."
        },
        "arxiv_html": {
          "type": "boolean",
          "description": "A LaTeXML rendering exists (post-2023 submissions). When false, links.html falls back to ar5iv."
        },
        "hf_indexed": {
          "type": "boolean"
        },
        "hf_upvotes": {
          "type": "integer"
        },
        "hf_claimed_authors": {
          "type": "integer"
        },
        "hf_claimed_by_me": {
          "type": "boolean",
          "description": "Whether YOU claimed it -- the actionable bit, not how many authors did."
        },
        "hf_github_repo": {
          "type": [
            "string",
            "null"
          ]
        },
        "hf_github_stars": {
          "type": [
            "integer",
            "null"
          ]
        },
        "hf_n_models": {
          "type": [
            "integer",
            "null"
          ]
        },
        "hf_n_datasets": {
          "type": [
            "integer",
            "null"
          ]
        },
        "canonical_page": {
          "type": [
            "string",
            "null"
          ],
          "description": "Set when a co-author or project site owns this paper's canonical page. We then LINK to it instead of generating a competing one -- see docs/RULES.md §9 on duplication."
        },
        "has_sidecar": {
          "type": "boolean"
        },
        "s2_author_record": {
          "type": "string"
        },
        "only_in_s2": {
          "type": "boolean",
          "description": "Present in Semantic Scholar but absent from the curated bibliography."
        },
        "merged_from": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "merged_by_override": {
          "type": "boolean"
        },
        "similar_but_distinct": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Needs a human decision, recorded in overrides.yaml as force_merge or force_distinct. Never auto-merged."
        },
        "metadata_problems": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "overridden_fields": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "owner": {
          "type": [
            "string",
            "null"
          ],
          "description": "Who owns this paper's canonical page and sidecar. Null means unclaimed. When it is not us, canonical_page points at theirs and we generate a link instead of a competing page."
        },
        "owner_source": {
          "type": "string",
          "enum": [
            "self",
            "peer",
            "config",
            "unclaimed"
          ],
          "description": "How ownership was determined: our own claim, a peer's published manifest, a local config override, or nobody has claimed it. This file is the only place a `self` claim is recorded, so `collect.py` carries this field and `owner`/`canonical_page`/`owner_conflict` across its rewrite rather than re-deriving them -- see `collect.carry_claims` for what a dropped claim costs."
        },
        "owner_conflict": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Set when more than one party claims this paper. Never auto-resolved -- a duplicated canonical page is the harm this whole mechanism exists to prevent."
        },
        "title_display": {
          "type": "string",
          "description": "Title with LaTeX braces and math wrappers resolved. Used in headings, highwire citation_title, and JSON-LD; `title` keeps the raw form for source matching."
        },
        "venue_display": {
          "type": "string",
          "description": "Venue trimmed at a word boundary for display."
        },
        "_override": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "extra_arxiv",
            "extra_openreview",
            null
          ],
          "description": "Which data/overrides.yaml list put this record in the corpus, for the papers the bibliography has not caught up with. Underscore-prefixed because it is provenance for a temporary state: the fix is one entry upstream, after which the line leaves overrides.yaml and the marker with it. It also disappears when the bibliography's own entry merges with this record, which is the same fact arriving the other way -- so the papers still carrying it are exactly the ones still absent upstream, which is what WORKLIST.md counts."
        }
      }
    }
  }
}
